import { Pool } from 'pg';
export declare const databaseConfig: {
    user: string;
    host: string;
    database: string;
    password: string;
    port: number;
};
export declare const pool: Pool;
